import React from 'react'
import Button from 'react-bootstrap/Button';

export default function ButtonBook({ textButton }) {
    return (
        <Button variant="outline-info">{textButton}</Button>
    )
}
